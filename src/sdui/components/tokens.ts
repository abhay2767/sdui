/**
 * Design tokens for SDUI components.
 *
 * Now a thin re-export of the app theme (`src/theme/`) so components inside
 * and outside the SDUI tree share one source of truth for color, typography,
 * spacing, and responsive scaling. Payloads reference colors by token name
 * (`"color": "primary"`) and may override with literal hex.
 */
export * from '../../theme';
